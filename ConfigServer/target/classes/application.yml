server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER
    
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PatelHarsh00008/micro-service-configuration
          clone-on-start: true